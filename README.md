# html-css-resume
Personal resume web document built using HTML and CSS as a practical application . The project demonstrates the use of semantic HTML elements and basic CSS for layout, styling, and responsiveness.
